from pathlib import Path

RAIZ = Path(__file__).resolve().parents[2]
FRONTEND = RAIZ / "frontend"
DADOS = RAIZ / "dados"
CATALOGO_JSON = DADOS / "catalogo.json"
ESTOQUE_JSON = DADOS / "estoque.json"
FOTOS = DADOS / "fotos"
